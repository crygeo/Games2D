<Window x:Class="Gusanito.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:Gusanito.ViewModel"
        xmlns:cnv="clr-namespace:Gusanito.Converters"
        xmlns:local="clr-namespace:Gusanito"
        mc:Ignorable="d"
        Title="MainWindow" WindowStartupLocation="CenterScreen" WindowStyle="None" AllowsTransparency="True"
        Background="Transparent" KeyDown="MainWindow_OnKeyDown">
    <Window.DataContext>
        <vm:MainVM />
    </Window.DataContext>
    <Window.Resources>
        <cnv:BoolToVisibilityConverter x:Key="BoolToVisibility" />
    </Window.Resources>
    <Grid VerticalAlignment="Center" HorizontalAlignment="Center">
        <Image Source="{Binding GameImage}" Stretch="Uniform"></Image>

        <!-- HUD -->
        <StackPanel VerticalAlignment="Top" Margin="10">
            <TextBlock Text="{Binding ScoreText}" FontSize="20" Foreground="White" />
            <TextBlock Text="{Binding TimeText}" FontSize="20" Foreground="White" />
        </StackPanel>

        <!-- Game Over -->
        <TextBlock Text="GAME OVER"
                   Visibility="{Binding IsGameOver, Converter={StaticResource BoolToVisibility}}"
                   FontSize="40"
                   Foreground="Red"
                   HorizontalAlignment="Center"
                   VerticalAlignment="Center" />

        <TextBlock Text="PAUSE"
                   Visibility="{Binding IsPaused, Converter={StaticResource BoolToVisibility}}"
                   FontSize="40"
                   Foreground="Red"
                   HorizontalAlignment="Center"
                   VerticalAlignment="Center" />
    </Grid>
</Window>